Library

เรียก streamlit
pip install streamlit pandas plotly

จับพิกัดภาพ
pip install streamlit-image-coordinates

วาดกรอบสี่เหลี่ยมทับรูปภาพให้เห็นกันชัดๆ ไปเลยครับ
pip install Pillow

เชื่อม gemini มาทำสรุป
pip install google-generativeai

วิธีรัน
streamlit run app.py